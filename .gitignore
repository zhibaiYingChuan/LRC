# Code Memory — .gitignore
# =========================

# Rust 编译产物
/target/
*.o
*.so
*.dylib
*.dll
*.exe
*.wasm
*.pdb
*.ilk

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 环境变量
.env
.env.local

# 模型文件（CodeBERT 等，由用户自行下载）
*.safetensors
*.onnx
*.pt

# 临时文件
*.tmp
*.log

# 调试会话记录（TRAE-debugger 产物，不交付用户）
debug-*.md
.dbg/

# 内部开发文档（不交付用户）
docs/evaluation-report-2026-05-31.md
docs/evaluation-report-*.md
docs/PRD_LRC_DESKTOP.md
docs/PRD_LRC_DESKTOP_V2.md

# 运行时数据目录
.loong-recall/
src/.loong-recall/
data/
data_*/

# 本地测试/调试脚本（不交付用户）
check_data.py
diagnose_recall.py
test_req.json
test_request.json
test_new_user.ps1
tests/e2e_test.ps1
tests/quick_test.ps1
tests/test5_only.ps1
tests/e2e_v*.ps1
tests/*_only.ps1
tests/new_user_flow.ps1

# 内部开发工具（不交付用户）
scripts/gatekeeper.ps1
scripts/install_hooks.py
scripts/pre-commit
scripts/pre-commit.ps1
scripts/patcher.py
scripts/sync_check.ps1
scripts/verify_fresh_install.ps1
scripts/convert_model.py

# 内部测试（不交付用户）
external_tests/
tests/dao_annotation_check.rs

# 内部架构文档（不交付用户）
docs/dao-pivot-mapping.md
docs/ALGORITHM_OVERVIEW.md
docs/COMMUNITY_GOVERNANCE.md

# 内部分析文档（不交付用户）
docs/product-analysis/

# 内部架构文档与测试报告（不交付用户）
docs/ARCHITECTURE.md
desktop/TEST_REPORT_v0.5.7.md
desktop/TEST_REPORT_*.md

# v0.6.0 迭代产生的本地审计/测试/修复报告（不交付用户）
docs/AUDIT_REPORT_*.md
docs/TEST_REPORT_*.md
docs/FIX_PLAN_*.md
docs/CROSS_PLATFORM_AUDIT_*.md

# v0.6.0 本地设计文档与UI Kit（不交付用户）
# 注：static/colors_and_type.css 和 static/components.css 是生产必需资源，由 server.rs include_str! 内嵌，必须推送
docs/LRC*全案*.md
loong-recall/
static/logo-preview.html

# v0.6.0 本地测试脚本（不交付用户）
desktop/test-full.mjs
desktop/test-*.mjs
test-cdp.ps1

# 桌面端二进制目录（构建产物，不入库）
desktop/src-tauri/binaries/
desktop/.github/

# 本地测试脚本（不交付用户）
test_server.ps1

# 图标生成工具（开发者本地使用，不交付用户）
scripts/generate_icon.py
scripts/generate_icons.py

# 桌面端构建产物（Tauri）
desktop/src-tauri/target/
desktop/node_modules/

# 模型文件目录（用户自行下载）
models/

# 性能分析文件
*.profraw
*.profdata
perf.data
flamegraph.svg

# 压缩文件
*.tar.gz
*.zip
*.7z

# macOS 应用签名
*.app/

# 代码签名相关（本地开发用，不入库）
*.p12
*.pem
*.key
*.crt
*.mobileprovision

# 桌面端测试脚本与产物（开发者本地使用，不交付用户）
desktop/test_webdriver.py
desktop/test_desktop_webdriver.py
desktop/tests/
desktop/tests/e2e_user_simulation.ps1
desktop/tests/e2e_results.json
desktop/tests/screenshots/
desktop/src/test_ui.html
desktop/src/wizard.test.js
desktop/src/*.test.*
*.test.js
*.test.ts

# 桌面端测试目录（开发者本地使用，不交付用户）
desktop-test/

# 桌面端图标生成工具与临时图标（开发者本地使用，不交付用户）
desktop/IcoGenerator.cs
desktop/gen_ico.ps1
desktop/generate_ico.ps1
desktop/hex_ico.ps1
desktop/update_icons.ps1
desktop/verify_ico.ps1
desktop/src-tauri/sign-binary.ps1
desktop/src/tauri-init.js
desktop/src-tauri/build-log.txt
desktop/src-tauri/icons/894.png
desktop/src-tauri/icons/894_real.png
desktop/src-tauri/icons/Square*.png
desktop/src-tauri/icons/StoreLogo.png
desktop/src-tauri/icons/android/
desktop/src-tauri/icons/ios/
desktop/src-tauri/icons/icon.icns
desktop/src-tauri/icons/64x64.png

# 根目录图标生成工具（开发者本地使用，不交付用户）
check_icon.ps1
generate_icons.ps1

# 项目级 AI 规则文件（每个用户/项目自动生成，不入库）
.trae/
.cursor/
.codebuddy/
.clinerules
.windsurfrules
.roo/
.comate/
.zed/

# 内部开发文档（不交付用户）
docs/evaluation-report-2026-05-31.md
docs/evaluation-report-*.md
docs/PRD_LRC_DESKTOP.md
docs/PRD_LRC_DESKTOP_V2.md
docs/v0.5.0_*.md
docs/v0.5.1_*.md
docs/v0.5.4_*.md
docs/v0.5.5_*.md
docs/audit-report-*.md
docs/GitHub仓库审计报告_*.md
docs/LRC_12模型产品深度分析报告.md
docs/产品功能汇报报告*.md
docs/产品战略汇报报告.md
docs/完整代码审计报告_*.md
docs/桌面生产环境就绪修复计划.md

# 根目录临时/工具脚本（开发者本地使用，不交付用户）
_*
-*
.git-commit-msg.txt
amend_push.ps1
check_git.ps1
compare.ps1
do_git.bat
do_git.ps1
run_check.ps1
build_release.ps1

# ============================================================
# v0.8.7 HCSE 发布合规评估后新增规则（2026-07-30）
# 依据: docs/PUSH_STANDARD.md + HCSE 发布合规专家评审
# ============================================================

# P0: 根目录 node_modules（之前仅忽略 desktop/node_modules/，遗漏根目录）
/node_modules/

# P1: 参赛材料（非产品交付，含 270MB 数据集超 GitHub 限制，通过 Release 单独发布）
competition/

# P1: 本地测试截图（非交付内容）
docs/desktop_test_screenshots/

# P1: 本地测试/验证脚本（仅开发期使用，非交付内容）
scripts/verify_*.ps1
scripts/test_*.py
scripts/test_*.js
scripts/test[0-9]*.py
scripts/test[0-9]*.js
scripts/cdp_*.py
scripts/probe_*.py
scripts/check_*_v0*.ps1
scripts/cleanup_old_builds_v0*.ps1
scripts/copy_sidecar_to_desktop_v0*.ps1
scripts/stop_processes_before_build.ps1
scripts/convert_onclick_to_data_action.ps1
scripts/audit_*.py
scripts/emergency_unblock.py
scripts/force_reload.py
scripts/commit_msg_*.txt
scripts/audit_report.json

# P1: 本地测试/开发数据（非交付内容，2026-09-14 v0.9.7 推送前清理）
# 依据: 这 4 个文件仅在本地用于联调与人工验证，全仓零引用（README/CI/测试均未引用），
#       且 CDP 与单元测试不依赖它们 —— 属"本地测试与开发文件"，不随克隆交付。
scripts/add_life_memories.ps1
scripts/test_association_quality.ps1
scripts/life_memories_batch.json
scripts/life_memories_batch2.json

# P1: 本地报告文档（内部审计/测试/评审报告，非用户文档）
docs/CDP_TEST_REPORT_*.md
docs/HCSE_VERIFICATION_REPORT_*.md
docs/INTERACTION_AUDIT_REPORT_*.md
docs/PRODUCT_ASSESSMENT_*.md
docs/PRODUCT_REVIEW_*.md
docs/PUSH_STANDARD_*_REVIEW.md
docs/DESKTOP_*_REPORT_*.md
docs/DESKTOP_*_PLAN_*.md
docs/RULES_AUDIT_*.md
docs/ONBOARDING_AUDIT_REPORT.md
docs/MEMORY_STORAGE_ASSESSMENT_*.md
docs/cdp_regression_*.js

# P1: 备份文件（不应入库）
*.bak
static/index.html.bak

# v0.8.25 发布合规 P0 阻断：旧版本构建产物（不应入库）
target_v0.8.20/

# v0.8.25 发布合规 P0 阻断：图标备份目录（不应入库）
desktop/src-tauri/icons_backup_*/

# v0.8.25 发布合规 P0 阻断：历史测试运行时产物（截图、JSON 结果，非交付内容）
evidence/
hcse_resilience_tester/

# v0.8.25 发布合规 V3：本地测试日志和 CI 产物（非交付内容）
e2e_logs.txt
e2e_raw.txt
get_jobs.jq
jobs.json
ubuntu_desktop_logs.txt
temp/sidecar_pid.txt

# v0.8.45 发布合规：本地开发运行目录与临时文档（非交付内容）
target-dev/
scripts/cdp_*.js
scripts/cdp-exec.ps1
scripts/cdp-exec2.ps1
scripts/dev-proxy.py
scripts/run-dev.ps1
scripts/run-dev.bat
PRODUCT-DOC.md
grill-state.json
docs/HCSE_RESILIENCE_AUDIT_REPORT_L6.md
docs/SESSION_*.md
docs/hcse_resilience_v0.8.42.md

# v0.9.0 发布清理：中文开发过程文档（不交付用户）
docs/修复计划_*.md
docs/审计报告_*.md
docs/*_优化计划*.md
docs/*_专项报告.md
docs/HCSE_RESILIENCE_AUDIT*.md
docs/hcse_resilience_*.md
docs/v0.*_optimization_plan*.md
docs/*专项报告*.md
docs/*并发安全*.md

# v0.9.0 发布清理：本地测试/临时目录（不交付用户）
temp/
tmp/

# 旧版本运行时与历史 CDP 归档（本地隔离，不参与构建和发布）
archive/legacy-runtime/

# v0.9.1 发布清理：本地开发文档（不交付用户）
docs/半可用演示态修复方案_*.md
docs/引导页重设计规范_*.md

# v0.9.1 发布清理：CDP 回归测试报告（不交付用户）
tests/frontend/cdp-regression-report.*

# v0.9.1 发布清理：开发/发布流程文档与测试脚本（不交付用户，本地开发自用）
docs/DEV_ISOLATION_SPEC.md
docs/HCSE_RELEASE_PROTOCOL.md
docs/PRE_PUSH_CHECKLIST.md
docs/PUSH_STANDARD.md
docs/RELEASE_PROCESS.md
docs/PRODUCT_ROADMAP_v1.0.md
tests/frontend/dashboard-smoke.spec.js
tests/frontend/playwright.config.js

# v0.9.1 发布清理：根目录开发残留（旧版预检脚本与 ws 测试依赖，不交付用户）
/preflight_check.ps1
/package.json
/package-lock.json

# 本地回归测试输出与调试残留（不交付用户）
test-output*.log
test-results/

# v0.9.7 发布清理（2026-09-14）：内部研发/审计过程文档（不交付用户）
# 依据：以下文档均为**内部过程产物**（代码审查报告、算法实验计划、状态机设计、
#       模型评测报告），面向维护者而非用户；经全仓引用反向扫描确认**零引用**
#       （README / CI / release.yml / 源码均不引用），属"本地开发测试文档"。
# 边界：Cargo 打包走 Cargo.toml 的 include/exclude，与本文件无关。
docs/GLOBAL_CODE_REVIEW_REPORT.md
docs/ASSOCIATION_ACCURACY_PLAN.md
docs/DAOTI_STATE_MACHINE_PLAN.md
docs/MODEL_EVALUATION.md
test-results-*/
tests/frontend/*-report.*
tests/frontend/screenshots/
tests/frontend/videos/
*-test-output.*
*-test-results.*
*-regression-report.*

# 本地调试与临时脚本（不交付用户）
debug_*.ps1
debug_*.py
debug_*.js
scripts/debug_*.ps1
scripts/debug_*.py
scripts/debug_*.js
scripts/local_*.ps1
scripts/local_*.py
scripts/local_*.js

# 本地开发过程文档（用户文档除外）
debug-*.md
docs/*测试*.md
docs/*调试*.md
docs/*开发*.md
docs/*回归*.md
docs/*验证*.md

# v0.9.6 道体-LRC 检索/消融实验中间产物（clean_v*.json、v8_retrieval_*.json 为一次性实验数据，不交付用户）
clean_v*.json
v8_retrieval_*.json

# 道体本地运行时、模型和 Python 缓存（源码脚本仍可按需单独纳入版本控制）
daoti/*.pt
daoti/__pycache__/
daoti/**/*.pyc

# 道体实验本地凭据与验证运行时数据（禁止入库）
daoti/api_key.txt
daoti/.t3-verify/
daoti/**/*.jsonl
technical_*materials*.jsonl
semantic_quality_*.json
verify_*_2026*.json

# v0.9.7 发布准备（用户要求：本地测试文件/开发文件/文档不入库）
# 根目录审查残留
.review_diff.txt
.review_*.txt
# 道体实验目录整体为本地研究资产（脚本+实验数据），不随产品发布
daoti/
# Playwright 运行产物（截图/DOM dump/日志）
playwright-artifacts/
# 本地开发流程脚本（归档工具、镜像连通性测试）
scripts/archive_legacy_runtime.ps1
scripts/test_external_model_mirrors.ps1
# 中文开发过程文档（设计/审计/跟踪类，不交付用户）
docs/仪表盘页面重构方案.md
docs/道体-LRC*.md
# v0.9.7 基准评测临时产物（本地开发/评测用，不入库）
src/bin/persistence_eval.rs
data_eval_v3*/
benchmarks/*_raw_*.json

# v0.9.7 修订（2026-09-14）：撤回 HCSE 清单的交付豁免
# 背景：上轮（r10）曾以"回写清单须随克隆交付"为由解除忽略（原为两条 ! 负向规则）。
#       后续判定：两份 HCSE 清单是**内部发布/韧性审计过程文档**，全仓零引用
#       （README 不引用、CI 不依赖），属"本地开发测试文档"，不应随产品仓库交付。
# 处置：移除负向规则，回落到下方 :325 / :347 的既有忽略规则；文档保留在本地磁盘。
# 说明：若将来需要交付，重新加回负向规则即可（须置于对应忽略规则之后）。

# v0.9.7 例外：本地开发链路脚本解除忽略（被已跟踪测试文件引用，须随克隆交付）
# 背景：下列 :303-305 的规则由 local-dev 于 v0.8.45 归入"本地开发运行目录（非交付内容）"，
#       但 3 个【已跟踪】测试文件依赖它们——"引用即契约"，被忽略即等同于克隆后必然失效：
#         tests/frontend/cdp-regression.js          （dev-proxy 探测/降级）
#         tests/frontend/association-dashboard-cdp.js（dev-proxy 优先，打包副本回退）
#         tests/frontend/association-desktop-cdp.js （dev-proxy 探测/降级）
#       且两份文档（HCSE_RELEASE_PROTOCOL 检查项 3）均无"如何获取"说明。
# 安全性：三脚本均无硬编码绝对路径（分别用相对路径 / Get-Location / %~dp0..），
#         无 token/secret 等敏感内容，dev-proxy 仅绑定本地回环用途。
# 边界（★v0.9.9 事实核查修正）：
#       **本文件不影响任何打包产物** —— 它只管 git 是否跟踪，与 Cargo / Tauri 打包无关。
#       ⚠ 原表述为「Cargo 打包走 Cargo.toml 的 include/exclude」，**该说法是错的**：
#         Cargo.toml 里**从来没有** include / exclude / files 任何一个键（已全仓核实）。
#         按 Rust 默认行为，`src/` 下所有被 `mod` 图可达的 `.rs` **都会进包**。
#         ⇒ 别再把"已由打包边界保护"当作免责依据（v0.9.9 实测结论，报告为本地维护资料，不入库）。
#       本例外仅使三脚本可提交，与打包无关。
# 顺序约束：负向规则必须置于其对应忽略规则之后，故集中于此，勿上移。
!scripts/dev-proxy.py
!scripts/run-dev.ps1
!scripts/run-dev.bat

# v0.9.8 发布清理（2026-09-16）：道体研究资产目录（不随产品发布）
# 依据：与上文 :422 `daoti/` 同性质——三者均为**道体研究的选型/源码/权重归档**，
#       面向维护者的研究材料，非产品交付内容；体积合计约 **91.4 MB**
#       （选型包 2.3MB / 融合引擎源码与文档 44.1MB / v15 权重包 45.0MB），
#       远超 GitHub 单文件与仓库体量惯例，且含模型权重（*.onnx / *.pt / .safetensors）。
# 边界（★v0.9.9 事实核查修正）：**本规则只阻止其进入 git 仓库，不删除本地文件，
#       也不影响打包产物** —— 本文件与 Cargo / Tauri 打包完全无关。
#       ⚠ 原表述为「Cargo 打包走 Cargo.toml 的 include/exclude」，**该说法是错的**：
#         Cargo.toml 里从来没有 include / exclude / files 键（已全仓核实）。
#       ★★另需注意一个**容易误读的点**：此处"道体研究资产不随产品发布"**只对这个目录成立**，
#          **不等于"道体算法不随产品发布"**。`src/engine/` 下这些自认 DaoTi Research License
#          的 Rust 模块是**同源的另一套实现，它会无条件参与编译并进入安装包**（已由二进制
#          字符串级实证：`lrc-sidecar.exe` 内含"承载基础/刚性法则/陷溺困境/愉悦表达"等
#          只存在于 `mirror_trapezoid.rs` 的宫义串，且这些串在前端资源里 0 命中）：
#            dao_evolution.rs / dao_metrics.rs / dao_regulator.rs / mirror_trapezoid.rs /
#            luoshu_encoder.rs / navigation.rs（+ ml 门控下的 luoshu_encoder_ml.rs）
#          实证与方法见 v0.9.9 实测结论（二进制字符串级实证；报告为本地维护资料，不入库）。
# 注：选型结论已提炼进 `daoti/PREREG_MEMORY_ASSOCIATION.md`（该文件亦在 daoti/ 忽略范围内）。
11_记忆联想专项_选型包/
DAOti+llm_融合引擎源码与文档/
daoti_v15_package/

# v0.9.8 本地研究过程文档（不交付用户，与 :394 docs/*验证*.md 同类）
docs/RECORD_LAYER_ROLLOUT.md

# v0.9.8：内部架构文档（不交付用户，与 :91 docs/ARCHITECTURE.md 同类）
# 依据：一份面向维护者的 LRC 全栈架构说明（354 行），经全仓引用反向扫描确认**零引用**；
#       且其文档基准为 v0.9.7（含"未提交的状态驱动发现改动"），已落后于当前版本，
#       随产品交付会立即产生文档漂移。
docs/ARCHITECTURE_OVERVIEW.md

# v0.9.8：过程验证报告（不交付用户）
# 依据：与上文 :271 `docs/CDP_TEST_REPORT_*.md`、:398 `docs/*验证*.md` 同类——
#       均为一次性验收/技术验证的过程留痕，非产品文档。
#       仅因文件名为英文而未被上述中文/模式规则覆盖，此处补齐口径。
docs/CDP_WHY_VERIFY_*.md
docs/DAOTI_INTEGRATION_VERIFY.md

# v0.9.9 发布清理（2026-09-22）：内部研发过程文档（不交付用户）
# 依据：以下 7 份均为**面向维护者的过程产物**（实测报告 / 代码审查报告 / 验收结果），
#       非产品文档；旧规则因文件名是英文而未被上述中文模式规则（docs/*验证*.md 等）覆盖，此处补齐口径。
#       本次一并 `git rm --cached` 取消跟踪，**本地文件保留**。
#   · G5A_QUOTA_COMPETITION_MEASUREMENT.md    — 配额竞争实测报告
#   · GOAL_DRIVEN_VALUE_MEASUREMENT.md        — 目标驱动价值测量报告
#   · MEMORY_ASSOCIATION_CODE_REVIEW.md       — 联想子系统代码审查报告
#   · SYMBOLIC_LAYER_INTEGRATION_ACCEPTANCE.md— 符号层接入验收标准（待确认稿）
#   · SYMBOLIC_LAYER_INTEGRATION_RESULT.md    — 符号层接入验收实测结果
#   · SYMBOLIC_LAYER_TWO_CRITERIA_MEASUREMENT.md — 符号层两条判据落地实测
#   · V099_INTERACTION_USABILITY.md           — v0.9.9 交互可用性实测报告
# 边界：其关键结论已内联到引用它们的源码注释中（`src/memory_store.rs` 两处、
#       `src/memory_store_tests.rs` 一处），移出文档**不影响代码可读性**。
# 注：**基准测试文档不在此列** —— `benchmarks/` 下全部报告按产品材料保留交付。
docs/G5A_QUOTA_COMPETITION_MEASUREMENT.md
docs/GOAL_DRIVEN_VALUE_MEASUREMENT.md
docs/MEMORY_ASSOCIATION_CODE_REVIEW.md
docs/SYMBOLIC_LAYER_INTEGRATION_ACCEPTANCE.md
docs/SYMBOLIC_LAYER_INTEGRATION_RESULT.md
docs/SYMBOLIC_LAYER_TWO_CRITERIA_MEASUREMENT.md
docs/V099_INTERACTION_USABILITY.md
