# LRC v0.8.25 优化推进方案 v4.0

> **基于三份最新审计报告综合制定（2026-08-02 第二轮审计）**：
> 1. HCSE 韧性验证审计（90% 置信度）— 4 项新发现 + 4 项回归确认
> 2. 项目入职审计（用户体验 B+ / 代码功能 A-）— 10 项缺陷
> 3. 发布规范合规审计（发布就绪）— 5 项信息性建议
> **版本**：v0.8.25 → **目标版本**：v0.8.26 | **日期**：2026-08-02

---

## 1. 执行摘要

### 1.1 审计总览

| 审计维度 | 审计师 | 总体结论 | 新发现缺陷 | 回归确认缺陷 |
|---------|-------|---------|-----------|------------|
| 交互韧性 | HCSE 韧性验证架构师 | 90% 置信度，7/7 不变式 PASS | 4 项（2xP2 + 2xP3） | 4 项（1xP2 + 3xP3） |
| 用户体验 + 代码功能 | 项目入职审计师 | 用户体验 B+ / 代码功能 A- | 10 项（2x中 + 8x低） | — |
| 发布规范 | 高可信发布规范专家 | 发布就绪，5 项信息性建议 | 5 项（全部 LOW/INFO） | — |
| **合计（去重后）** | | | **约 15 项** | **4 项** |

### 1.2 当前版本状态

| 维度 | 状态 |
|------|------|
| 版本号一致性 | ✅ 9/9 全部一致（v0.8.25） |
| CI/CD 配置 | ✅ 全部 PASS（Actions SHA 锁定、权限最小化、harden-runner 全覆盖） |
| P0 阻断项 | ✅ 0 项 |
| P1 高优项 | ✅ 0 项 |
| P2 改进项 | ⚠️ 4 项（建议本迭代修复） |
| P3 跟踪项 | ⚠️ 11 项（可延后或分批次修复） |

---

## 2. 完整审计项追溯表

### 2.1 P2 改进项（建议发布前修复）

| ID | 问题 | 来源 | 严重级别 | 涉及文件 | 预估工时 | 修复建议 |
|----|------|------|---------|---------|---------|---------|
| NEW-02 | **postMessageToParent 超时后 Tauri invoke 继续执行** | 韧性验证 | P2 | `static/app.js` | 2h | 与 `postMessageToParent` 不同，Tauri `invoke` 命令无法通过 `Promise.race` 取消。考虑在 Tauri 命令中增加 `abort_signal` 参数，或在 commands.rs 中实现超时检查 |
| NEW-04 | **contains_whole_word 边界检查跳过可能误匹配** | 韧性验证 | P2 | `desktop/src-tauri/src/agent_detector.rs` | 0.5h | 在末尾检查中添加 null 字节(0x00)判断，确保 .lnk 内容末尾的边界不会跳过子串匹配 |
| CODE-02 | **/v1/model/test cancel_flag 无法真正中断正在执行的编码** | 入职审计 | P2 | `src/v1_api.rs` | 2h | 在 `encode_text` 内部增加对取消标志的定期检查，或评估 `spawn_blocking` 替代方案 |
| UX-01 | **"测试链接"按钮文字歧义** | 入职审计 | P2 | `static/index.html` | 0.3h | 将"测试链接"改为"测试镜像源"，与"测试模型"和"测试连接"形成清晰区分 |

### 2.2 P3 跟踪项（可分批修复）

| ID | 问题 | 来源 | 涉及文件 | 修复建议 | 预估工时 |
|----|------|------|---------|---------|---------|
| NEW-01 | `_lockBusyCooldown` 标志在标签页切换时未重置 | 韧性验证 | `static/app.js` | 标签页切换时重置冷却期状态 | 0.5h |
| NEW-03 | `_retryModalActive` 退避延迟期间标签页切换竞态 | 韧性验证 | `static/app.js` | 退避延迟开始时记录当前标签页 ID，重试时验证一致性 | 0.5h |
| UX-02 | testEmbedderConnection 未使用 setButtonState | 入职审计 | `static/app.js` | 统一使用 setButtonState 管理按钮状态 | 0.5h |
| UX-03 | 版本号硬编码窗口期 | 入职审计 | `static/index.html`, `static/app.js` | 初始状态显示 "--" 等待后端返回 | 0.5h |
| UX-04 | 船长日志 success 反馈 try/catch 太静默 | 入职审计 | `static/app.js` | 在 catch 中添加 `console.warn` 日志 | 0.3h |
| UX-05 | backupMemories 部分失败缺少 Toast | 入职审计 | `static/app.js` | 部分失败时同时调用 showToast | 0.3h |
| UX-06 | 向导在浏览器中无法完整使用 | 入职审计 | `static/index.html`, `static/app.js` | 对依赖 Tauri 的按钮显示禁用状态 + tooltip | 1h |
| CODE-01 | Trae CN 检测路径可能不完整 | 入职审计 | `desktop/src-tauri/src/agent_detector.rs` | 增加对 `%LOCALAPPDATA%\Programs\` 下子目录的扫描 | 0.5h |
| CODE-03 | testEmbedderConnection 按钮状态恢复未重置颜色 | 入职审计 | `static/app.js` | 统一使用 setButtonState | 0.3h |
| CODE-04 | 超时保护未覆盖所有 IO 操作 | 入职审计 | `desktop/src-tauri/src/commands.rs` | 为 `auto_upgrade_configs` 和 `write_rules_for_agents` 添加 10s 超时 | 0.5h |
| REL-03 | CHANGELOG 版本号未纳入 preflight 自动检查 | 发布合规 | `.github/workflows/release.yml` | 在 preflight 版本号检查中增加 CHANGELOG.md 检查 | 0.3h |
| REL-04 | index.html 仅 1 处版本号被 preflight 覆盖 | 发布合规 | `.github/workflows/release.yml` | 补充检查 `#sys-version` 和 `#status-version` | 0.3h |
| REL-05 | 本地 Rust 1.94.0 vs CI 1.97.0+ | 发布合规 | 无 | 本地升级 Rust 或使用 `rust-toolchain.toml` 锁定版本 | 0.3h |

### 2.3 延后项（v0.9.0+）

| ID | 问题 | 来源 | 原因 |
|----|------|------|------|
| V3-03 | 网络审计运行时拦截器 | 韧性验证 | 架构级变更，需在 v0.9.0 中作为独立特性实现 |
| REL-01 | CodeQL 安全分析缺失 | 发布合规 | 建议性，非发布阻断 |
| REL-02 | 依赖审查缺失 | 发布合规 | 建议性，非发布阻断 |

---

## 3. 修复路线图

### 3.1 阶段一：P2 必修复项（共 4.8h）

| 步骤 | ID | 修改内容 | 涉及文件 | 工时 | 风险 |
|------|----|---------|---------|------|------|
| Step 1 | UX-01 | "测试链接" → "测试镜像源" | `static/index.html` | 0.3h | 无 |
| Step 2 | NEW-04 | contains_whole_word 边界检查修复 | `agent_detector.rs` | 0.5h | 低（修改范围明确） |
| Step 3 | CODE-02 | cancel_flag 定期检查机制 | `src/v1_api.rs` | 2h | 中（需修改编码器内部逻辑） |
| Step 4 | NEW-02 | postMessageToParent 超时取消机制 | `static/app.js` + `commands.rs` | 2h | 中（需前后端协调） |

### 3.2 阶段二：P3 顺手修复项（共 5h）

| 步骤 | ID | 修改内容 | 涉及文件 | 工时 |
|------|----|---------|---------|------|
| Step 5 | NEW-01 | `_lockBusyCooldown` 标签页切换重置 | `static/app.js` | 0.5h |
| Step 6 | NEW-03 | `_retryModalActive` 标签页竞态修复 | `static/app.js` | 0.5h |
| Step 7 | UX-02 | testEmbedderConnection 使用 setButtonState | `static/app.js` | 0.5h |
| Step 8 | UX-03 | 版本号初始显示 "--" 等待后端返回 | `static/index.html`, `static/app.js` | 0.5h |
| Step 9 | UX-04 | 船长日志 catch 添加 console.warn | `static/app.js` | 0.3h |
| Step 10 | UX-05 | backupMemories 部分失败 Toast | `static/app.js` | 0.3h |
| Step 11 | CODE-03 | testEmbedderConnection 按钮颜色恢复 | `static/app.js` | 0.3h |
| Step 12 | CODE-04 | IO 操作添加 10s 超时 | `commands.rs` | 0.5h |
| Step 13 | REL-03 | preflight 增加 CHANGELOG 版本号检查 | `release.yml` | 0.3h |
| Step 14 | REL-04 | preflight 补充 #sys-version/#status-version 检查 | `release.yml` | 0.3h |
| Step 15 | REL-05 | 本地 Rust 版本升级 | 无 | 0.3h |
| Step 16 | CODE-01 | Trae CN 检测路径扩展 | `agent_detector.rs` | 0.5h |
| Step 17 | UX-06 | 浏览器环境向导禁用提示 | `static/index.html`, `static/app.js` | 1h |

### 3.3 阶段三：回归测试 + 发布（共 2h）

| 步骤 | 内容 | 说明 |
|------|------|------|
| Step 18 | cargo test + cargo clippy | 验证后端测试 |
| Step 19 | preflight_check.ps1 | 15 项检查全通过 |
| Step 20 | WebView2 CDP 交互测试 | 验证前端交互链路 |
| Step 21 | 版本号升级 + 推送 | 升级到 v0.8.26 |

---

## 4. 交付标准

### 4.1 发布就绪条件

- [x] 版本号一致性：9/9 全部一致
- [x] CI/CD 配置：全部 PASS
- [x] P0 阻断项：0 项
- [x] P1 高优项：0 项
- [ ] P2 改进项：4 项全部修复
- [ ] P3 改进项：建议修复 13 项（至少修复 8 项核心项）
- [ ] 验证测试：cargo test + cargo clippy + preflight_check.ps1 通过
- [ ] 交互韧性测试：WebView2 CDP 测试通过
- [ ] 发布规范审计：PASS

### 4.2 验证方法

1. 修复后运行 `cargo test --features server` 验证后端测试
2. 运行 `cargo clippy --features server -- -D warnings` 验证代码质量
3. 运行 `scripts/preflight_check.ps1` 验证 15 项检查
4. 启动 sidecar 验证 `/v1/health/system` 返回 version 字段
5. 调用 `POST /v1/model/test` 验证模型测试端点
6. 前端验证：加载页面确认版本号显示、模型测试按钮、向导流程正常

---

## 5. 风险评估

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| CODE-02 cancel_flag 修复影响编码器性能 | 低 | 中 | 仅在不阻塞的检查点添加取消标志检查，不影响主编码路径 |
| NEW-02 postMessageToParent 修复需 Tauri API 变更 | 中 | 中 | 优先使用前端侧方案（AbortController + 忽略过期响应），后端变更作为备选 |
| 发布前修复过多导致版本号滞后 | 低 | 低 | 严格按工时评估，P2 修复控制在 5h 内 |
| UX-06 浏览器环境修复可能引入桌面端兼容问题 | 低 | 低 | 通过 `window.__TAURI__` 检测环境，桌面端行为不变 |

---

## 6. 附录：审计报告引用

### 6.1 HCSE 韧性验证 — 7 项安全不变式

| 不变式 | 结果 | 说明 |
|--------|------|------|
| INV-001 数据一致性 | PASS | 三阶段锁安全模式 |
| INV-002 UI 安全 | PASS | handleHttpError 错误分类全覆盖 |
| INV-003 超时保护 | PASS | 16/16 超时路径验证通过 |
| INV-004 状态恢复 | PASS | 三阶段锁模式，失败不回滚 |
| INV-005 资源隔离 | PASS | HashMap 按项目隔离 |
| INV-006 取消安全 | PASS | AtomicBool 取消标志（需确认 CODE-02 修复后） |
| INV-007 版本号一致性 | PASS | 异步获取 + 静默降级 |

### 6.2 发布合规 — 版本号一致性

| 检查点 | 文件 | 值 | 状态 |
|--------|------|-----|------|
| 1-9 | 全部 9 处 | 0.8.25 | ✅ 全部 PASS |
| 额外 | index.html #sys-version | v0.8.25 | ✅ PASS |
| 额外 | index.html #status-version | v0.8.25 | ✅ PASS |
| 额外 | CHANGELOG version | 0.8.25 | ✅ PASS |

### 6.3 入职审计 — 核心发现

| 类别 | 评级 | 关键发现 |
|------|------|---------|
| 用户体验 | B+ | 按钮文字歧义、状态管理不一致、版本号窗口期 |
| 代码功能 | A- | cancel_flag 无法真正中断、超时未覆盖所有 IO |
| 配置正确性 | A | 版本号 9/9 一致、CI/CD 完整、.gitignore 完备 |