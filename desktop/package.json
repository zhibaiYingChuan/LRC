{
  "name": "lrc-desktop",
  "private": true,
  "version": "0.9.4",
  "description": "LRC Desktop — 本地优先的 AI 代码记忆桌面应用",
  "type": "module",
  "scripts": {
    "dev": "cross-env LRC_DEV_MODE=1 tauri dev",
    "build": "tauri build",
    "preview": "tauri build --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-shell": "^2",
    "ws": "^8.21.0"
  }
}
