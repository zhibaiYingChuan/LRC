# LRC 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|---------|
| v0.8.x | ✅ 活跃维护 |
| v0.7.x | ⚠️ 仅安全修复 |
| < v0.7 | ❌ 不再支持 |

## 报告漏洞

如果您发现 LRC 项目中的安全漏洞，请通过以下方式报告：

1. **不要公开披露** — 在修复之前，请勿在公开渠道（Issues、Discussions、社交媒体）讨论漏洞
2. **发送邮件** 至项目维护者（通过 GitHub Issues 创建安全报告）
3. **预期响应时间**：我们将在 48 小时内确认收到报告，并在 7 天内提供修复计划

### 报告内容应包括

- 漏洞描述
- 复现步骤（包括环境版本信息）
- 预期影响
- 可能的修复建议（可选）

## 安全实践

### 代码安全

- 所有外部输入在渲染前必须经过转义/脱敏
- API 密钥使用 aes-gcm 加密存储
- CSP 配置限制脚本来源
- 敏感操作需要用户确认

### 供应链安全

- 所有 GitHub Actions 使用 Commit SHA 固定版本
- 依赖通过 Cargo.lock 锁定
- 定期更新依赖以修复已知漏洞

### 数据安全

- 记忆数据默认本地存储，不离开用户设备
- 支持完整的备份/恢复/导出功能
- 哈希链防篡改审计日志

## 安全更新

安全更新将通过在 CHANGELOG.md 中标注 `[安全]` 前缀来标识。